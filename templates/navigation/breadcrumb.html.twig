{% if breadcrumb %}
  {% set breadcrumbTemplate = directory ~ '/templates/uikit/breadcrumb/breadcrumb.twig' %}
  {% set defaultBreadcrumbTemplate = '@govcms8_uikit_starter/uikit/breadcrumb/breadcrumb.twig' %}
  {%
    include [breadcrumbTemplate, defaultBreadcrumbTemplate] with {
      items: breadcrumb
    } only
  %}
{% endif %}
